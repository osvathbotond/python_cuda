#pragma once


void add(float* vec1, float* vec2, float* res, const size_t vector_length);
void sub(float* vec1, float* vec2, float* res, const size_t vector_length);
float norm(float* vec, const float p, const size_t vector_length);
